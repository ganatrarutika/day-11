function calculateAverage(num1, num2, num3) {
    const sum = num1 + num2 + num3;
    const average = sum / 3;
    return average;
  }
  const avg = calculateAverage(10, 15, 20);
  console.log(avg);
  